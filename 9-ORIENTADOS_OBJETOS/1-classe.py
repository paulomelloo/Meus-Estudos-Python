class Pessoa: # criamos a classe Pessoa
    def __init__(self, nome, idade): # init (inicialização do objeto que vamos criar). Objetos 'self' (ele mesmo) pessoa, criado o objeto 'nome' e objeto 'idade'
        self.nome = nome # propriedade do objeto nome
        self.idade = idade #propriedade do objeto idade
